# Files excluded from the distributed plugin (WordPress.org).
# Runtime stays: src/, assets/, languages/, rapls-sitemap.php, uninstall.php,
# readme.txt.

/.git
/.github
/.gitignore
/.distignore
/.claude
CLAUDE.md

/docs
/tests
/bin
/node_modules
*.zip

/composer.json
/composer.lock

.DS_Store
*.log
